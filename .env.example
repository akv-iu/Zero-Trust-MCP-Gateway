# v1 requires NO secrets. This file exists so v1.1 does not invent a new pattern.
# GROQ_API_KEY is read by agent/provider.py ONLY (AGENT-005) and must never appear
# in [child].env_allowlist.
GROQ_API_KEY=
